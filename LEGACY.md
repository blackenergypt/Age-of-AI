# Código legado (pré-monorepo)

A lógica foi partida para:

- `apps/api`
- `apps/game-server`
- `apps/web`
- `apps/panel`

Ver `docs/ARCHITECTURE.md`.

Podes apagar `server/` e `public/` na raiz quando confirmares que `pnpm dev` cobre o teu fluxo.
